<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.brookmanholmes.billiardmatchanalyzer"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.CreateNewMatchActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".ui.MatchInfoActivity"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.MainActivity"/>
        <activity
            android:name=".ui.PlayerProfileActivity"
            android:screenOrientation="portrait"
            android:label="Player Profile"/>
        <activity android:name=".ui.IntroActivity">
        </activity>
    </application>

</manifest>